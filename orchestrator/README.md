# WorkFlowy Orchestrator

LLM-агентный оркестратор, использующий WorkFlowy как единый source of truth.
Агенты читают задачи, ведут диалоги, и пишут результаты обратно в дерево.

## Архитектура

```
┌─────────────────────────────────────┐
│         ТЫ (собственник)            │
│  Стратегия, партнёрства, решения    │
└──────────────┬──────────────────────┘
               │ дайджесты + эскалации
               │
┌──────────────▼──────────────────────┐
│       ORCHESTRATOR (main.py)        │
│  Polling → Classify → Dispatch      │
└──┬────────┬────────┬────────────────┘
   │        │        │
┌──▼──┐ ┌──▼──┐ ┌──▼──────┐
│Claude│ │YaGPT│ │ Custom  │
│agent │ │agent│ │ agents  │
└─────┘ └─────┘ └─────────┘
   │        │        │
   └────────┴────────┘
            │ read/write
   ┌────────▼────────┐
   │   WorkFlowy     │
   │   (Beta API)    │
   └─────────────────┘
```

## Конвенции WorkFlowy-дерева

### Структура

```
- 🏛 СОБСТВЕННИК
  - Видение, стратегия, партнёрства...

- 🔄 ОПЕРАЦИОНКА
  - 📋 Backlog ← backlog_node_id в конфиге
    - Задача #status:backlog #agent
    - Задача #status:review #agent
    - Задача #status:done ✅
  - 📬 На ревью ← review_node_id
  - 📊 Дайджесты ← digest_node_id

- 📥 INBOX
```

### Теги задач

| Тег | Значение |
|-----|----------|
| `#status:backlog` | Новая задача, ждёт обработки |
| `#status:in-progress` | Агент работает |
| `#status:review` | Агент сделал, ждёт ревью человека |
| `#status:done` | Завершено |
| `#status:blocked` | Нужно решение человека |
| `#agent` | Назначена агенту (любому) |
| `#dev-agent` | Назначена dev-агенту |
| `#comms-agent` | Назначена comms-агенту |
| `#human` | Назначена человеку |
| `#automate` | Зелёная зона — делать без ревью |
| `#review` | Жёлтая — делать, показать |
| `#decide` | Красная — только подготовить |

### Уровни автономии

```
🟢 GREEN  — агент делает сам, отмечает done
🟡 YELLOW — агент делает, кладёт на review (default)
🔴 RED    — агент готовит материал, ждёт решения
```

Автоматическая классификация по ключевым словам (см. `orchestrator.py`).
Можно явно задать тегами `#automate` / `#review` / `#decide`.

### Эмуляция Fractal Conversations

Диалог = вложенные items с эмодзи-префиксами:

```
- Задача: Добавить onboarding #status:review #agent
  - 🤖 Сделал три варианта. Trade-offs:
    - Вариант А: простой, нет персонализации
    - Вариант Б: с квизом, +2 дня
    - Вариант В: полная персонализация, +5 дней
  - 👤 Вариант Б, но квиз максимум 3 вопроса
    - 🤖 Понял. Уточнение: формат ответов — кнопки или свободный текст?
      - 👤 Кнопки
        - 🤖 Готово. Реализовал: [описание]. Проверь.
```

**Ветвление (branching):**

```
- 🤖 Вот 3 варианта: А, Б, В
  - 👤 Вариант Б
    - 🤖 Уточнение по Б...
  - 🤖 Кстати, по варианту А есть нюанс...  ← ветка на уровень выше
```

Агент может ответить не только на последнее сообщение, но и на более раннее —
создавая параллельную ветку (метод `agent_branch()`).

**Определение состояния:**
- Последний leaf = 🤖 → ждём человека (awaiting_human)
- Последний leaf = 👤 → ждём агента (awaiting_agent)
- `#resolved` / `#done` в тексте → завершён

### Дайджест

Генерируется в узел `digest_node_id`:

```
📊 Дайджест — 2026-02-20 08:00
🟢 Выполнено автоматически: 3
  • Обновить статусы тестов
  • Рефакторинг утилит
  • Генерация документации
🟡 На ревью: 2
  • Новый onboarding flow
  • Аналитика по пилоту
🔴 Ждёт решения: 1
  • Ответ партнёру по срокам
💬 Ответов в диалогах: 4
⏰ Зависли (>24ч): 1
  • Интеграция с CRM (последний: 🤖)
```

## Установка

```bash
pip install httpx
```

## Настройка

```bash
cp config.example.json config.json
# Заполнить API ключи и UUID узлов WorkFlowy
```

UUID узлов — в DOM WorkFlowy (инспектор) или через API:
```bash
curl -X POST https://beta.workflowy.com/api/beta/list-children/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_KEY" \
  -d '{"item_id": "None"}'
```

## Запуск

```bash
# Непрерывный polling (каждые 5 минут)
python main.py

# Одиночный тик + дайджест в stdout
python main.py --once

# Записать дайджест в WorkFlowy
python main.py --digest

# Указать другой конфиг
python main.py --config /path/to/config.json
```

## Расширение

### Свой агент

```python
from agents import AgentRegistry

async def my_analytics_agent(task, context: str) -> str:
    # Твоя логика: сбор метрик, анализ, отчёт
    return "Вот отчёт: ..."

agents = AgentRegistry()
agents.register("analytics", my_analytics_agent)
```

### Свой routing

```python
from agents import make_claude_agent, make_routing_agent

router = make_routing_agent({
    "код": make_claude_agent(key, system_prompt="Ты разработчик..."),
    "текст": make_claude_agent(key, system_prompt="Ты копирайтер..."),
    "аналити": make_claude_agent(key, system_prompt="Ты аналитик..."),
})
agents.register("default", router)
```

## Файлы

```
workflowy_client.py  — API-клиент (CRUD + tree traversal)
dialog.py            — Система диалогов (эмуляция Fractal Conversations)
orchestrator.py      — Оркестратор (polling, dispatch, autonomy, digest)
agents.py            — Агенты (Claude, YandexGPT, routing, custom)
main.py              — Entry point
config.example.json  — Шаблон конфига
```

## Будущее

Когда WorkFlowy расширит beta API:
- [ ] Переключить диалоги на нативные Fractal Comments
- [ ] Использовать webhooks вместо polling
- [ ] Добавить поиск/фильтрацию через API вместо tree traversal
- [ ] search-items endpoint для delta-sync
